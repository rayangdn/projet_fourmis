#include <iostream>
#include <vector>
#include <cmath>

using namespace std;


class Fourmiliere {
	void calcul_taille();
	void destruction_fourmiliere();
	private :
	unsigned int sizeF;
	//Carre carre; module squarecell
	//vector<Fourmis> fourmis;
	
};
int main() {
	
	return 0;
}
void Fourmiliere::calcul_taille() {
//module fourmis
//	sizeF = sqrt(4*((sizeG*sizeG)+(sizeC*sizeC*nbC)+(sizeD*sizeD*nbD)+(sizeP*sizeP*nbP));
}
//Test effectuer dans squarecell pour la taille et la superposition
void Fourmiliere::destruction_fourmiliere() {
	//if fourmi generator existe plus
	//end_of_klan = true;
	//supprimer_carre; module squarecell
}
