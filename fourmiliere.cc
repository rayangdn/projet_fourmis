#include <iostream>
#include <vector>
#include <cmath>

using namespace std;


class Fourmiliere {
	void calcul_taille();
	
	private :
	unsigned int sizeF;
	//Carre carre; module squarecell
	//vector<Fourmis> fourmis;
	
};
int main() {
	
	return 0;
}
void Fourmiliere::calcul_taille() {
//module fourmis
//	sizeF = sqrt(4*((sizeG*sizeG)+(sizeC*sizeC*nbC)+(sizeD*sizeD*nbD)+(sizeP*sizeP*nbP));
}
//Test effectuer dans squarecell pour la taille et la superposition

